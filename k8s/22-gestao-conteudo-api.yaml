apiVersion: v1
kind: ConfigMap
metadata:
  name: peo-gestao-conteudo-api-config
  namespace: peo
data:
  ASPNETCORE_ENVIRONMENT: Development
  ConnectionStrings__DefaultConnection: Data Source=../data/gestao-conteudo.db;
---
apiVersion: v1
kind: Secret
metadata:
  name: peo-gestao-conteudo-api-secrets
  namespace: peo
type: Opaque
stringData:
  ConnectionStrings__messaging: amqp://guest:guest@rabbitmq:5672
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: peo-gestao-conteudo-api
  namespace: peo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: peo-gestao-conteudo-api
  template:
    metadata:
      labels:
        app: peo-gestao-conteudo-api
    spec:
      containers:
      - name: peo-gestao-conteudo-api
        image: mba-projectfive-peo-gestao-conteudo-api:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
        volumeMounts:
        - name: peo-data
          mountPath: /data
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        envFrom:
        - configMapRef:
            name: peo-gestao-conteudo-api-config
        - secretRef:
            name: peo-gestao-conteudo-api-secrets
      volumes:
      - name: peo-data
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: peo-gestao-conteudo-api
  namespace: peo
spec:
  selector:
    app: peo-gestao-conteudo-api
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  type: ClusterIP
